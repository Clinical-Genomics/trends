language: python

python:
    - '3.6'

install:
    - pip install cython
    - pip install -r requirements.txt -r requirements-dev.txt .
    - pip check

script:
    - py.test -v tests/*

after_success:
    - coveralls

notifications:
    email: false
